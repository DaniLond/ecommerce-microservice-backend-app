# CI/CD Pipeline for cloud-config
name: cloud-config CI/CD

on:
  push:
    branches: [develop, master]
    paths: ['cloud-config/**', '.github/workflows/cloud-config.yml']
  pull_request:
    branches: [develop, master]
    paths: ['cloud-config/**']
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options: [development, staging, production]

jobs:
  deploy-to-development:
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-microservice-ci-cd.yml
    with:
      service_name: 'cloud-config'
      service_port: '9296'
      context_path: '/'
      environment: 'development'
    secrets: inherit

  deploy-to-staging:
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-microservice-ci-cd.yml
    with:
      service_name: 'cloud-config'
      service_port: '9296'
      context_path: '/'
      environment: 'staging'
    secrets: inherit

  deploy-to-production:
    if: github.event_name == 'workflow_dispatch' && inputs.environment == 'production'
    uses: ./.github/workflows/reusable-microservice-ci-cd.yml
    with:
      service_name: 'cloud-config'
      service_port: '9296'
      context_path: '/'
      environment: 'production'
    secrets: inherit
