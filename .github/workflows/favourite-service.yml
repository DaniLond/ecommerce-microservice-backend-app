# CI/CD Pipeline for favourite-service
name: favourite-service CI/CD

on:
  push:
    branches: [develop, master]
    paths: ['favourite-service/**', '.github/workflows/favourite-service.yml']
  pull_request:
    branches: [develop, master]
    paths: ['favourite-service/**']
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options: [development, staging, production]

jobs:
  deploy-to-development:
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-microservice-ci-cd.yml
    with:
      service_name: 'favourite-service'
      service_port: '8800'
      context_path: '/favourite-service'
      environment: 'development'
    secrets: inherit

  deploy-to-staging:
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-microservice-ci-cd.yml
    with:
      service_name: 'favourite-service'
      service_port: '8800'
      context_path: '/favourite-service'
      environment: 'staging'
    secrets: inherit

  deploy-to-production:
    if: github.event_name == 'workflow_dispatch' && inputs.environment == 'production'
    uses: ./.github/workflows/reusable-microservice-ci-cd.yml
    with:
      service_name: 'favourite-service'
      service_port: '8800'
      context_path: '/favourite-service'
      environment: 'production'
    secrets: inherit
